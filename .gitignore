# Large synthetic dataset (regenerable; not source)
dataset/*.csv

# The deployed app loads these at startup, so they are TRACKED (small):
#   models/food_model.joblib, models/segment_model.joblib,
#   models/feature_schema.json, models/weights.yaml
# Only the training-report artifacts are ignored:
models/metrics.json
models/comparison.csv

.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Reference-only material (not part of the deployed app)
notebooks/
dataset/
